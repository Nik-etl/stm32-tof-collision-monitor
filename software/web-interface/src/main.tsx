import React from 'react'
import ReactDOM from 'react-dom/client'
import IrobotController from './IrobotController'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <IrobotController />
  </React.StrictMode>,
)